{
  "name": "lyrical-graphql-monorepo",
  "private": true,
  "scripts": {
    "start": "lerna run start --stream",
    "dev": "lerna run dev --stream",
    "prod": "lerna run start-prod --stream",
    "test": "lerna run test --stream",
    "lint": "lerna run lint --stream"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  },
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
